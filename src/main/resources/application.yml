server:
  port: 8500
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/amroczkowski/altkom-animal-hotel-configuration-docker
          clone-on-start: true
